services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: kgtutor
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kgtutor"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:${POSTGRES_PASSWORD}@postgres:5432/kgtutor
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      LLM_MODEL: ${LLM_MODEL:-gemini-2.0-flash}
      LLM_CLASSIFIER_MODEL: ${LLM_CLASSIFIER_MODEL:-gemini-2.0-flash}
      LLM_BASE_URL: ${LLM_BASE_URL:-https://generativelanguage.googleapis.com/v1beta/openai/}
      EMBEDDINGS_MODEL: ${EMBEDDINGS_MODEL:-paraphrase-multilingual-MiniLM-L12-v2}
      CONFIDENCE_THRESHOLD: ${CONFIDENCE_THRESHOLD:-0.7}
      MERGE_THRESHOLD: ${MERGE_THRESHOLD:-0.85}
      MAX_CHAPTER_TOKENS: ${MAX_CHAPTER_TOKENS:-4000}
      GRAPHRAG_MAX_ENTITIES: ${GRAPHRAG_MAX_ENTITIES:-15}
      ENTITY_LINK_THRESHOLD: ${ENTITY_LINK_THRESHOLD:-0.6}
      QUESTIONS_PER_CONCEPT: ${QUESTIONS_PER_CONCEPT:-3}
      LEARNED_SCORE_THRESHOLD: ${LEARNED_SCORE_THRESHOLD:-0.7}
      # Прокси для доступа к Gemini API (хостовый прокси через host.docker.internal).
      # Значение из .env (GEMINI_PROXY), а не из HTTP_PROXY хоста — иначе
      # подхватится 127.0.0.1 от VS Code, недоступный внутри контейнера.
      # postgres резолвится по имени сервиса, поэтому исключаем его из прокси.
      HTTP_PROXY: ${GEMINI_PROXY:-http://host.docker.internal:10801}
      HTTPS_PROXY: ${GEMINI_PROXY:-http://host.docker.internal:10801}
      NO_PROXY: postgres,localhost,127.0.0.1
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./uploads:/app/uploads

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     VITE_API_URL: http://localhost:8000
  #   depends_on:
  #     - backend

volumes:
  pgdata:
